templates:
  # Template specific
  - name: NumSetReversed
    use_template: NumSet
    sets:
      - param: index
      - param_reversed: true
      
  - name: NumSetParam
    use_template: NumSet
    sets:
      - param: index

  - 
    name: NumSet
    use_template: ""
    script: NumSet
