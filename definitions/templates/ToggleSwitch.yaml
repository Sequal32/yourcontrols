# Template
mappings:
  - name: A:FUEL TANK CENTER LEVEL
    vars:
      - name: A:FUEL TANK CENTER LEVEL
        units: Percent
    period: Second

# Template
# Recurse until getting to type == last_type
templates:
  # Mapping specific
  - name: A:BRAKE PARKING POSITION
    use_template: ToggleSwitchParam
    vars:
      - name: BRAKE PARKING POSITION
        units: Bool
    sets:
      - name: SET_PARKING_BRAKE
  # Template specific
  - name: ToggleSwitchParam
    use_template: ToggleSwitch
    sets:
      - param: index

  - 
    use_template: ""
    name: ToggleSwitch
    script: ToggleSwitch
